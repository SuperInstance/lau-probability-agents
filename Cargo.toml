[package]
name = "lau-probability-agents"
version = "0.1.0"
edition = "2021"
description = "Probability theory as the foundation for agent reasoning under uncertainty"
license = "MIT"

[dependencies]
nalgebra = "0.33"
num-complex = "0.4"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
approx = "0.5"
